# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのリポジトリで作業する際のガイダンスを提供します。

## プロジェクト概要

**Agentravel** は、旅行のインスピレーションを詳細な旅程に変換するAIエージェントアプリケーションです。ユーザーが「上海ディズニーに行きたい」のような簡単な入力をすると、システムがフライト、宿泊、観光地、レストラン、最適化されたスケジュールを含む完全な複数日の旅行計画を生成します。

**ターゲットユーザー**: 詳細な計画が欲しいが、手動で計画を立てる時間がない個人旅行者。

**コアバリュー**: 漠然としたインスピレーション → 詳細で美しい旅程への変換

## 技術スタック

- **フロントエンド**: TanstackStart
- **AIオーケストレーション**: Mastra
- **LLM**: GPT-5（テキスト + 画像生成）
- **バックエンド**: Cloudflare Workers
- **データベース**: Cloudflare D1（SQLite）
- **外部API**: Google Distance Matrix API、Web検索API
- **パッケージマネージャー**: pnpm

## 開発フェーズ

プロジェクトは3フェーズのMVPアプローチに従います：

- **Phase 0**: セットアップと接続確認（1-2日）
- **Phase 1**: エージェント構築（7-10日） - Mastra Playgroundでエージェントを構築・テスト
- **Phase 2**: UIを含む完全なアプリ実装（10-14日）

フェーズに縛られず、必要に応じて実装してください。

## アーキテクチャ

### マルチエージェントシステム

システムはハイブリッドアーキテクチャを使用：**メインエージェント（オーケストレーター）** が **5つのサブエージェント** を調整：

1. **フライト検索エージェント**（手順5-6）: フライトオプションを検索・提案
2. **宿泊検索エージェント**（手順8）: 日別プランに基づいてホテルを推薦
3. **観光スポット検索エージェント**（手順9）: 各日の観光地をリストアップ
4. **グルメ検索エージェント**（手順10）: 食事のレストランを提案
5. **ルート最適化エージェント**（手順11-12）: 訪問順序を最適化し、詳細なタイムラインを作成

### メインエージェント

- **パターン**: ハイブリッド（手順1-8は固定フロー + 手順9-13はReAct）
- **責務**:
  - ユーザー対話（手順1-4、7、13）
  - 状態管理（AgentMemory）
  - サブエージェントのオーケストレーション
  - 検証と調整の判断
- **ReActループ**: Thought → Action → Observation → 完了まで繰り返し
- **エラーハンドリング**: 自動的に代替案を提案（例：フライトが利用できない場合、異なる日程を提案）
- **並列実行**: 可能な場合（例：同じ日のスポット + グルメ検索）

### ツール

すべてのエージェントが共有ツールを使用：
- **Web検索**: スポット、ホテル、レストランの情報収集
- **Google Distance Matrix API**: 移動時間と距離を計算
- **DB操作**: セッション/プランの永続化
- **画像生成**: GPT-5がスポット画像を生成

## データベーススキーマ

Cloudflare D1（SQLite）を5つのテーブルで使用：

1. **users**: 匿名ユーザー管理（uuidv7）
2. **sessions**: `memory_snapshot`（JSON）を含むエージェントセッション状態
3. **plans**: 完成した旅行プラン
4. **days**: 旅行の各日（day_number、area、theme）
5. **activities**: 個別のアクティビティ（観光、食事、移動、ホテル）

主要な設計決定：
- 柔軟性のためのJSON保存（`memory_snapshot`、`metadata`）
- IDにuuidv7を使用
- CASCADE削除（plan → days → activities）
- URL形式：`/users/{userId}/plans/{planId}`

完全なSQLスキーマは `docs/database-schema.md` を参照。

## 状態管理

### AgentMemory構造

メインエージェントは以下を含む `AgentMemory` オブジェクト（TypeScript型）を維持：
- セッションメタデータ（session_id、user_id、current_step、status）
- 会話履歴
- 決定事項（Layer 1：目的、目的地、日程、フライト、ホテル、日別コンセプト）
- 日配列（Layer 2：アクティビティを含む詳細スケジュール）

### 永続化戦略

**保存タイミング**：
- 重要な決定時（フライト確定、ホテル確定、各日完了、プラン完成）
- 30秒ごとの自動保存
- エラー発生時

**クラス**：
- `MemoryManager`: メモリ操作のヘルパー
- `SessionPersistence`: DB保存/読み込み、自動保存タイマー、プラン作成を処理

実装の詳細は `docs/state-management.md` を参照。

## ユーザージャーニー

旅行計画は13ステップに従います：
1. 目的の明確化
2. 目的地の決定
3. 時期の決定
4. 期間の決定
5. フライト検索
6. 日程の確定
7. 日別コンセプトの決定（各日のエリア + テーマ）
8. 宿泊先検索
9. 観光スポット検索
10. レストラン検索
11. ルート最適化（営業時間、食事時間を考慮したTSP問題）
12. 詳細タイムライン作成（移動時間を含む）
13. 検証と調整

システムは手戻りと反復的な改善をサポートします。

## 開発セットアップ

### 初期セットアップ
```bash
# 依存関係のインストール
pnpm init
pnpm add @tanstack/start mastra wrangler

# D1データベースの作成
wrangler d1 create agentravel-db

# スキーマの適用
wrangler d1 execute agentravel-db --file=./docs/database-schema.md
```

### APIキー
以下の環境変数を設定：
- OpenAI APIキー（GPT-5）
- Google Distance Matrix APIキー
- Web検索APIキー（TBD：Perplexity/Tavily/Google Search）

### Phase 1: エージェントのテスト
UI統合前にMastra Playgroundでエンドツーエンドテストを実施。

### Phase 2: デプロイ
- **バックエンド**: Cloudflare Workers（wrangler deploy）
- **フロントエンド**: Cloudflare Pagesまたは同等

## 主要な設計決定

### 会話スタイル
自然な対話形式 - LLMが必要な情報を判断し、硬直したフォームではなく自然に質問します。

### サブエージェントの呼び出し
情報が揃ったら即座に実行（パターンA） - ユーザーを確認待ちさせません。

### エラーハンドリング
自動的に代替案を提案（パターンB） - 検索が失敗した場合、異なるオプションを試してユーザーに提示します。

### データフォーマット
硬直したスキーマよりも柔軟性のためのJSON保存 - MVP期間中の反復が容易です。

### MVPスコープ
MVPから除外：
- 興味タグ
- 「詰め込みすぎ」警告
- 代替案生成ボタン
- PDFエクスポート
- カレンダーエクスポート
- 地図表示
- 認証機能（Phase 3以降）

## コスト見積もり

旅程あたり（概算）：
- GPT-5テキスト生成：$0.20（10Kトークン）
- 画像生成：$0.16（4画像）
- Distance Matrix：$0.10（20回呼び出し）
- **合計：約$0.46/旅程**

## 重要なファイル

- `docs/index.md` - ドキュメントインデックス
- `docs/overview.md` - プロジェクト概要
- `docs/agent-architecture.md` - マルチエージェントシステム設計
- `docs/main-agent-flow.md` - 会話例を含む詳細なReActフロー
- `docs/database-schema.md` - SQLを含む完全なスキーマ
- `docs/state-management.md` - メモリ管理の実装
- `docs/user-journey.md` - 13ステップの計画プロセス
- `docs/development-plan.md` - タスク付きのフェーズ分解

## 開発哲学

- **MVP優先**: Phase 2が最小限の実用製品 - それ以外はすべてPhase 3以降に延期
- **柔軟性**: JSON保存により迅速な反復が可能
- **ユーザー体験**: ストリーミング表示、自然な会話、サブエージェントの即座の呼び出し
- **シンプルさ**: MVPには最小限必要なテーブル、複雑なチェックポイント機能なし
